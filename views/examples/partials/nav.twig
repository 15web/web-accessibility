{% set exampleItems = [{
        link: '#link-path-1',
        title: 'Элемент 1'
    },{
        link: '#link-path-2',
        title: 'Элемент 2'
    },{
        link: '#link-path-3',
        title: 'Активный элемент 3'
    }] %}

{% set navOptions = {
    items: exampleItems,
    activeLink: '#link-path-3'
} %}

{% set verticalNavOptions = {
    items: exampleItems,
    activeLink: '#link-path-3',
    isVertical: 'true'
} %}

{{ macros.download_lnks('nav', ['css', 'min.css', 'scss'], null, 'Ссылки для загрузок') }}

{{ macros.html(macros.nav(navOptions)) }}
<br>
{{ macros.html(macros.nav(verticalNavOptions)) }}
