{% macro formExample() %}
    <form action="url/to/action" class="form" aria-labelledby="feedback-form-title">
        <h2 id="feedback-form-title">Форма обратной связи.</h2>

        <!-- Содержимое формы -->
    </form>
{% endmacro %}

{% macro text() %}
    <input type="text" value="Иван" placeholder="Введите ваше имя">
    <br><br>
    <input type="email" value="" placeholder="Введите адрес электронной почты">
    <br><br>
    <input type="search" placeholder="Введите строку для поиска">
    <br><br>
    <input type="number" placeholder="Сколько вам полных лет?">
    <br><br>
    <input type="text" placeholder="Ваш любимый напиток?" disabled>
{% endmacro %}

{% macro textRequired(value, placeholder, isStrict) %}
    <input type="text" value="{{ value }}" placeholder="{{ placeholder }}" {{ isStrict ? 'required' : 'aria-required="true"' }}>
{% endmacro %}

{#
+ text/email/search/number
range
checkbox
radio
range
password
submit/reset/button
color
date
#}

{% import _self as view %}

<p>Чтобы стили применялись к элементам формы, у нее нужно указать <code>class="form"</code>.</p>
<p>Так же мы рекомендуем озаглавливать форму и указывать через атрибут <code>aria-labelledby</code> идентификатор заголовка формы.</p>
{{ macros.html(view.formExample()) }}

<form action="/" onsubmit="return false;" class="form">
    <p>Поля для ввода текста, электронной почты, числа и поиска имеют одинаковые стили отображения и одинаковое <code>disabled</code> состояние.</p>
{{ macros.html(view.text()) }}
</form>
